language: node_js

cache:
  yarn: true
  directories:
    node_modules

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.12
  - export PATH="$HOME/.yarn/bin:$PATH"

install:
  - yarn install
  
script:
  - "echo implement tests!"

before_deploy: "echo starting deploy!"
deploy:
  on:
    branch: master
  run:
    "echo must implement deploy script later!"